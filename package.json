{
  "name": "telegram-bot-vercel",
  "version": "1.0.0",
  "description": "Telegram bot with webhook, hosted on Vercel",
  "main": "api/webhook.js",
  "scripts": {
    "set-webhook": "node scripts/set-webhook.js",
    "delete-webhook": "node scripts/delete-webhook.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  },
  "optionalDependencies": {
    "@vercel/kv": "^1.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
